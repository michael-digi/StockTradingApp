// routes for user, session, and stock queries
const userRoutes = require('./user');
const sessionRoutes = require('./session');
const stockRoutes = require('./stocks')

module.exports = { userRoutes, sessionRoutes, stockRoutes }